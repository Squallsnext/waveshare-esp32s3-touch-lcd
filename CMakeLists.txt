# This is the project CMakeLists.txt file for the esp32s3-touch-lcd firmware
# to be used with ESP-IDF build system.

cmake_minimum_required(VERSION 3.16)

project(waveshare-esp32s3-touch-lcd VERSION 1.0.0)

# The toolchain file provided by ESP-IDF will set the compiler correctly
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project_begin()
project_end()
