version: "1.0.1"
description: "Waveshare ESP32-S3 Touch LCD 4.3\" (4.3 / 4.3B / 4.3C) - ESP-IDF + EEZ UI"

dependencies:
  idf: ">=5.3"
  # LVGL core (pin to 9.4.x series)
  lvgl/lvgl: "9.4.*"
  # ESP LVGL port (v2.x provides LVGL9 support)
  espressif/esp_lvgl_port: "^2.0.0"
  # Optional Waveshare BSP (uncomment if your board matches the BSP)
  # waveshare/esp32_s3_touch_lcd_4_3c: "^1.0.0"
  # NOTE: The following driver component names may vary or be provided
  # by the BSP / ESP-IDF tree. If build fails, either enable the BSP
  # above or add the appropriate components via `idf.py add-dependency`.
  # esp_lcd_rgb_panel: "latest"
  # esp_lcd_touch_gt911: "latest"
  # esp32_io_expander (CH422G): "latest"

component_version: "1.0.1"
